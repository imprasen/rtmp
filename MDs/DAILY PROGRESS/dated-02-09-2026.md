# Daily Progress — Dated 02-09-2026

**Project:** RTMP Live Streaming Server Deployment  
**Date:** 02 September 2026 (02-09-2026)  
**Location:** Remote via Tailscale SSH & Web HTTPS  
**Target Server:** Server 0149 (`172.16.3.50`)  
**Network Gateway:** `172.16.3.1` (Firewall)  
**Domain:** `live.dhanushuav.com`  

---

## 1. Objectives of the Day
1. **Fix "Origin not allowed" Login Issue:** Resolve CORS and origin validation on Server 0149 for Tailscale IP (`100.118.109.5`), local dashboard (`3000`), and public domain (`live.dhanushuav.com`).
2. **WAN Firewall Port Forwarding:** Create dedicated port forwarding (DNAT) rules on the Firewall for Server 0149 without modifying or reusing existing rules (Rules 1–9 reserved for other servers like `172.16.3.39`).
3. **Cloudflare DNS Configuration:** Configure the A record in Cloudflare DNS for `live.dhanushuav.com` pointing to the Tata Tele WAN IP (`14.97.37.70`).
4. **End-to-End Testing:** Verify external RTMP video publishing and web dashboard playback from outside the local network.

---

## 2. Actions & Investigations

### A. "Origin not allowed" Investigation
- **Root Cause:** In `rtmp-streamer/server/src/index.ts`, CORS whitelist defaults to `["http://localhost:3000", "http://localhost:5173"]`. `ALLOWED_ORIGINS` was not passed through `docker-compose.yml`, so requests with origin `http://100.118.109.5:3000` or the public domain were rejected by Express with `403 Forbidden: Origin not allowed`.
- **Resolution:** Added `ALLOWED_ORIGINS` to `.env` and `docker-compose.yml`, dynamic origin support for configured domain, and updated `mediamtx.yml` allowed origins.

### B. WAN Port Allocation Strategy
- Existing port 80 and 443 are currently mapped to `172.16.3.39`.
- New ports assigned for Server 0149 (`172.16.3.50`):
  - **RTMP Ingest:** Port `1935` (WAN `1935` → LAN `172.16.3.50:1935`)
  - **Web Dashboard & API:** Port `8443` (WAN `8443` → LAN `172.16.3.50:3000` or Caddy)
  - **WebRTC Signaling/Media:** Port `8889` (WAN `8889` → LAN `172.16.3.50:8889` TCP/UDP)

---

## 3. Verification & Test Results
- **Tailscale Web Login Test:**
  - URL: `http://100.118.109.5:3000/login`
  - Status: **SUCCESS** ✅
  - "Origin not allowed" error completely resolved.
  - Session authentication & JWT cookie established properly.
- **Public Web Access Test:**
  - URL: `http://live.dhanushuav.in:8443/`
  - Status: **SUCCESS** ✅ (Firewall DNAT WAN `8443` ──► LAN `172.16.3.50:3000` is active and working).
- **OBS RTMP URL Formatting:**
  - Separated Server URL (`rtmp://live.dhanushuav.in:1935/live`) and Stream Key (`11111`) in UI to prevent OBS invalid URL errors.
- **Clean HTTPS Access Test:**
  - URL: `https://live.dhanushuav.in/`
  - Status: **SUCCESS** ✅ (Port 8443 removed via Cloudflare Origin Rules; free Universal SSL active).
- **RTMP Ingest Verification:**
  - Status: **SUCCESS** ✅ (PotPlayer playing `rtmp://rtmp.dhanushuav.in:1935/live/48862` smoothly; 720p 30fps stream verified via MediaMTX paths API).
- **Web Player Black Screen Root Cause Identified:**
  - When browser requested `/hls/live/48862/index.m3u8`, MediaMTX returned `302 Found` to `/live/48862/index.m3u8?cookieCheck=1`.
  - Nginx did not have a proxy route for `/live/`, causing the browser to fall back to React `index.html`. HLS.js failed to parse HTML as video.
  - Added `location /live/` proxying directly to MediaMTX:8888, added `proxy_redirect / /hls/`, and added auto-fallback from WebRTC to HLS.

- **Cloudflare Free Tier Compliance Audit:**
  - Strict compliance verified: 100% Free Tier features only.
  - DNS Management: Free (unlimited queries).
  - Universal SSL (Edge TLS): Free (included with Free plan).
  - Origin Rules (Port rewrite 443 ──► 8443): 1 rule used (Free plan allows up to 10 rules).
  - Self-Hosted Video: All video compute, encoding, storage, and egress runs on Server 0149 (zero Cloudflare Stream fees).
  - RTMP Ingest (`rtmp.dhanushuav.in`): Grey Cloud (DNS Only), pure DNS translation with zero bandwidth processing fees.

---

## 4. Next Steps
1. Rebuild client on Server 0149 (`docker compose build client && docker compose up -d`).
2. Verify live video playback in browser on `https://live.dhanushuav.in/watch/48862`.




